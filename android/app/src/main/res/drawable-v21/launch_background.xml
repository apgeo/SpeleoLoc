<?xml version="1.0" encoding="utf-8"?>
<!-- Every supported device resolves this -v21 variant, so it must mirror
     drawable/launch_background.xml — the base file alone never shows. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="?android:colorBackground" />

    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/speleo_loc_1" />
    </item>
</layer-list>
